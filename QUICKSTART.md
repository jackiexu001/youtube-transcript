# 小白快速开始

## 第一次抓取

1. macOS 从网站下载 DMG，把应用拖入“应用程序”；Windows 下载 Setup.exe 并按安装向导完成安装。发布版都不需要安装 Python 或 yt-dlp。
2. macOS 打开 `开始抓取 YouTube 字幕.app`；Windows 从开始菜单打开“YouTube 字幕抓取”。两者都会直接显示图形界面，不会出现命令窗口。
3. 粘贴 YouTube 博主主页链接，例如 `https://www.youtube.com/@xxxx`。
4. 本次数量默认 100；如果遇到限流，下次改成 50。填 0 表示全部，不建议在几百条的大频道第一次运行时使用。
5. 确认“档案位置”。应用会记住选择；再次选择原来的档案文件夹即可继续增量更新。
6. 点击“开始抓取”。下方“运行日志”会从左侧开始、自动换行并实时显示视频标题、频道位置和运行提示；过程中可以点击“暂停”，稍后再点“继续”。完成后程序会自动打开刚抓好的博主页面，也可以点“打开结果”。

## 继续抓同一个频道

再次双击 `开始抓取 YouTube 字幕.app`，输入同一个博主主页即可。程序会自动跳过已完成视频，继续往更早的视频抓。

## 看已有结果

打开主应用，确认档案位置，然后点击“打开结果”。旧档案里的 `.command`、`.bat` 或小型查看器可以保留，但新版不再依赖也不会继续生成它们。
