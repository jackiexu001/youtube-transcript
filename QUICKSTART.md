# 小白快速开始

## 第一次抓取

1. macOS 从网站下载 DMG，把应用拖入“应用程序”；Windows 下载 Setup.exe 并按安装向导完成安装。发布版都不需要安装 Python 或 yt-dlp。
2. macOS 打开 `YouTube Transcript.app`；Windows 从开始菜单打开“YouTube Transcript”。两者都会直接显示图形界面，不会出现命令窗口。
3. 如需 AI 字幕，粘贴对应服务商的 API Key 后点击“保存 Key”；显示“已保存”后再开始抓取。
4. 粘贴 YouTube 博主主页链接，例如 `https://www.youtube.com/@xxxx`。
5. 本次数量默认 100；如果遇到限流，下次改成 50。填 0 表示全部，不建议在几百条的大频道第一次运行时使用。
6. 确认“档案位置”。应用会记住选择；再次选择原来的档案文件夹即可继续增量更新。
7. 默认勾选“无 YouTube 字幕时自动生成 AI 字幕（支持中文、英语等 99+ 种语言）”。请选择 Groq 或 OpenAI，粘贴对应 API Key；“获取 API Key”会打开官方平台。Mac 会安全存入钥匙串，Windows 会使用当前账户加密保存。
8. “网络代理”通常留空，应用会自动读取系统设置；只有浏览器能打开 YouTube 而应用仍连接失败时，才填写代理软件提供的本机代理地址。
9. 点击“开始抓取”。应用会先检查 YouTube 连接；下方“运行日志”会实时显示视频标题、频道位置、AI 音频切片、识别进度、重试和错误类型。过程中可以点击“暂停”，稍后再点“继续”。完成后程序会自动打开刚抓好的博主页面，也可以点“打开结果”。

## 继续抓同一个频道

再次打开 `YouTube Transcript.app`，输入同一个博主主页即可。程序会自动跳过已完成视频，继续往更早的视频抓。

如果 macOS 搜索出现两个同名应用，请先推出仍然挂载的安装磁盘并删除下载的 DMG；旧版“开始抓取 YouTube 字幕”可以从“应用程序”文件夹删除。

macOS 菜单栏的“设置 → 主题”提供跟随系统、浅色和深色三种模式，默认跟随系统。

如果上次因为断网、限流或单个视频异常而中断，失败项目已经保存在待重试队列中；网络恢复后仍然只需输入同一个主页，程序会优先重试，不需要寻找失败视频。

## 看已有结果

打开主应用，确认档案位置，然后点击“打开结果”。旧档案里的 `.command`、`.bat` 或小型查看器可以保留，但新版不再依赖也不会继续生成它们。
